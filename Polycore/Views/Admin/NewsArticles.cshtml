@model IEnumerable<Polycore.Models.NewsArticleModel>
@{
    ViewBag.Title = "News articles index";
    if (!User.IsInRole("Administrator"))
    {
        Layout = "~/Views/Shared/_Error.cshtml";
    }
}
<div class="span12">
    <div class="template-wrapper">
        <div class="block block-page_header_block span12 first ">
            <div class="span12 block-title centered">
                <h2>Manage news articles</h2>
                <p>The info and stuff, right?</p>
            </div>
            <div class="span12 block-divider"></div>
        </div>
    </div>
    <div class="block block-column_block span12 first cf">
        <!-- Latest news -->
        <div class="block span12 first cf">
            <div class="title-wrapper">
                <h3 class="widget-title">
                    <i class="fa fa-user"></i>
                    News articles
                </h3>
                <div class="clear"></div>
            </div>
            <div class="wcontainer">
                <table class="table table-striped table-condensed table-hover" id="select">
                    <thead>
                        <tr>
                            <th>Title</th>
                            <th>User name</th>
                            <th>Published</th>
                            <th></th>
                        </tr>
                    </thead>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Title</td>
                            <td>@item.User.UserName</td>
                            <td>@item.Published</td>
                            <td>
                                @Html.ActionLink(" ", "AddNewsArticle", "Admin", new { id = item.NewsArticleID }, new { @class = "fa fa-fw fa-info" })
                                @Html.ActionLink(" ", "AddNewsArticle", "Admin", new { id = item.NewsArticleID }, new { @class = "fa fa-fw fa-users" })
                                @Ajax.ActionLink(" ", "DeleteNewsArticle", new { id = item.NewsArticleID }, new AjaxOptions
                                {
                                    Confirm = "Are you sure the news article with title: " + item.Title + " must be deleted?",
                                    HttpMethod = "Post",
                                    OnSuccess = "window.location.reload()"
                                }, new { @class = "fa fa-fw fa-trash-o" })
                            </td>
                        </tr>
                    }
                </table>
                <br />
                @Html.ActionLink("Back", "Dashboard", "Admin", null, new Dictionary<string, object>() { { "class", "button-green button-small" } })
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/Scripts/jquery.unobtrusive-ajax.min.js")
    @Scripts.Render("~/Scripts/jquery.js")
}
