@model IEnumerable<Polycore.Models.SubjectModel>

@{
    ViewBag.Title = "Subjects";
}

<h2>@ViewBag.Platform --> @ViewBag.Game --> Subjects</h2>

@foreach (var item in Model)
{
    <div> - @Html.ActionLink(item.Name, "Forum", new { id = item.SubjectID })</div>
}